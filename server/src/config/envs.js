import * as dotenv from "dotenv";
dotenv.config();

export const { USER_POSTGRET, USER_PASSWORD, HOST, PORT, NAME_DB } =
  process.env;

// export default {
//   USER_POSTGRET,
//   USER_PASSWORD,
//   HOST,
//   PORT,
//   NAME_DB,
// };
